%h2 All users

- @users.each do |user|
  %div
    = link_to( user.username, user_path(user.username) )
    = link_to( 'Delete', user, :method => :delete, :data => {:confirm => "Are you sure you want to delete #{user.username}?"})
    = link_to( 'Edit', edit_user_path(user.username))