%h2 All users

- @users.each do |user|
  %div
    = link_to( user.username, user_path(user.username) )
    = link_to( 'Delete', user_path(user.username), :method => :delete, :data => {:confirm => "Are you sure you want to delete #{user.username}?"}) if @current_user.is_admin?
    = link_to( 'Edit', edit_user_path(user.username)) if @current_user.is_admin?