%h2 All items

- @items.each do |item|
  %div
    = link_to( item.name, user_item_path( item.user.username , item.id) )
    = link_to( 'Delete', item, :method => :delete, :data => {:confirm => "Are you sure you want to delete #{item.name}?"}) if @current_user.is_admin? || item.user == @current_user
    = link_to( 'Edit', edit_item_path(item.id)) if @current_user.is_admin? || item.user == @current_user