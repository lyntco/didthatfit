<!DOCTYPE html>
%html
%head
  %title DID THAT FIT
  = stylesheet_link_tag('application', media: 'all', 'data-turbolinks-track' => true)
  = javascript_include_tag('application', 'data-turbolinks-track' => true)
  = csrf_meta_tags
%body
  %nav
    %ul
      = smartnav.html_safe
      %li= render :partial => '/pages/searchform'

  %div#wrap
    %div#content

      = yield

      - if flash[:notice].present?
        %div.notice
          = flash[:notice]

  %footer
    LINKS LINKS
    = link_to( "&copy; LYNT.CO".html_safe, 'http://blog.lynt.co' )
